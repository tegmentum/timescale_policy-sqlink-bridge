# timescale_policy × sqlink bridge (Phase 9.2, timescaledb)

Auto-generated by [`sqlink-shim-codegen`](https://github.com/tegmentum/sqlink-shim-codegen)
via `--dynlink --target timescale_policy --provider-id timescale_core-composed`.

## What this is

A `wasm32-wasip2` cdylib that imports `compose:dynlink/linker@0.1.0`
and exports the canonical `sqlite:extension@1.0.0` contract. At LOAD time
the host's provider registry resolves `timescale_core-composed` to the
timescaledb per-sub composed shim provider vendored at
`datafission/extensions/timescaledb/deps/timescaledb-core-provider-composed.wasm`.

## Host wiring

```sh
export SQLINK_SUB_EXT_BRIDGES="\
timescale_policy=$(pwd)/target/wasm32-wasip2/release/timescale_policy_sqlite_bridge_dynlink.wasm"

export SQLINK_SUB_EXT_PREBUILT="\
timescale_core=$HOME/git/datafission/extensions/timescaledb/deps/timescaledb-core-provider-composed.wasm"

export SQLINK_SUB_EXT_ALIAS="\
timescale_policy=timescale_core"

sqlink -c "LOAD timescale_policy;"
```

## Do not hand-edit

Regenerate from the source-of-truth interface DB + catalog via
`sqlink-shim-codegen`. Changes made directly here are lost on the
next regen.
